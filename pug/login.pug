<!DOCTYPE html>
html(lang="en")
    head
        include includes/head.pug
        link(rel="stylesheet", href="./style/login.css")
    body
        include includes/header.pug
        .login-container       
            form.form.login-form(action="/login" method="POST" class=error ? 'error-login-form' : '')
                input(type="text" name="login" placeholder="Your login" required)
                .password-container
                    input(type="password" name="password" id="loginPassword" placeholder="Your password" required)
                    .eye-icon(onclick="togglePassword('loginPassword', this)")
                        .eye
                        .eye-slash

                button(type='submit') Login

                if error
                    p.error #{error}

                div.login 
                    p You not have account?
                    a(href='/register') Register

        script(src='/js/password.js')
        script(src='/js/click.js')